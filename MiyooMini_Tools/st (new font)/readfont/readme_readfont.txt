readfont : A command line tool that reads ttf and displays font11x15.h style text

usage: readfont [ttf_filename(def:VCR_OSD_MONO_1.001.ttf)] [pointsize(def:20)] > log.txt

The log in default state is attached as log.txt
For actual use, the height and position need to be adjusted
